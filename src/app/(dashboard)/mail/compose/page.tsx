import { ComposeContent } from './_components/compose-content';

export const metadata = {
  title: 'Mail Compose - Vendor Portal',
  description: 'Compose a new message',
};

export default function ComposePage() {
  return <ComposeContent />;
}
