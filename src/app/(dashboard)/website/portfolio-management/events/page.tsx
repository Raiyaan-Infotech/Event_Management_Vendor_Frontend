import EventsHighlightsForm from "../_components/events-highlights-form";

export const metadata = {
  title: "Add Event Highlight | Vendor Portal",
};

export default function EventsHighlightsPage() {
  return <EventsHighlightsForm />;
}