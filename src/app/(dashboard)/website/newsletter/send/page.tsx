import SendMailContent from "../_components/send-mail-content";

export const metadata = {
  title: "Send Newsletter | Vendor Portal",
  description: "Create and send newsletter campaigns to your subscribers.",
};

export default function SendNewsletterPage() {
  return <SendMailContent />;
}
