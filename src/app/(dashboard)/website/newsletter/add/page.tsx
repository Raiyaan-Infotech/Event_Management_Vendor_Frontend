import AddSubscriberForm from "../_components/add-subscriber-form";

export const metadata = {
  title: "Add Subscriber | Vendor Portal",
  description: "Add a new subscriber to your newsletter list.",
};

export default function AddSubscriberPage() {
  return <AddSubscriberForm />;
}
