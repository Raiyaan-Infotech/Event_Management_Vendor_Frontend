import { VendorInboxContent } from './_components/vendor-inbox-content';

export const metadata = {
  title: 'Inbox - Vendor Portal',
  description: 'Manage your inbox and communications',
};

export default function InboxPage() {
  return <VendorInboxContent />;
}
